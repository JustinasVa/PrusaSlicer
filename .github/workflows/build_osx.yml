name: Build osx

# Note (surface-slicing fork): push trigger disabled — needs Prusa-internal
# secrets, fails on forks. Manual dispatch only.
on:
  workflow_dispatch:

jobs:
  build_osx:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/build_osx.yml@legacy
    secrets: inherit
    with:
      actions_branch: legacy
